#include<stdio.h>
int main()
{
int num,word,i,j;
char words[n][20],temp[i];
printf("Enter the number of words:");
scanf("%d",&num);
printf("Enter the word");
for(i=0;i<num;i++)
{
scanf("%[^\n]",word[i]);
}
for(i=0;i<n;i++)
{
for(j=i+1;j<n;j++)
{
if(strcmp(word[j],word[j+1]<0)
{
strcpy(temp[i],word[i]);
strcpy(temp[j],word[i]);

